module\_6\_network\_and\_calculator

needed cmake minimum 3.1 version

1. first of all build. Open cmd, 
mkdir build \&\& cd build
cmake ..
cmake --build . --config Release
cd Release 
2. First start server(receiver)
receiver\_server.exe
3. Then start sender
calculator\_sender.exe




===

